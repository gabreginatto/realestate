fastapi==0.111.0
uvicorn[standard]==0.29.0
transformers==4.41.0
# torch is pre-installed on the GCP deep-learning VM image — do not pin here
# to avoid downgrading the CUDA-built version. Install manually if running locally:
#   pip install torch==2.3.0
Pillow==10.3.0
numpy==1.26.4
python-multipart==0.0.9
